# plugin-unit

The `unit:` check verb for OpenCharly — a systemd unit **file** that has no cross-init
analogue: a `.socket`, `.target`, `.slice`, or a drop-in fragment.

Scaffold. The verb itself lands via PR.
